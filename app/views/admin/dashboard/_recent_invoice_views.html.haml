
- views = InvoiceView.order('created_at desc').limit(5).includes(:invoice)
- if views.length > 0
  %ul
    - views.each do |view|
      %li
        Invoice ##{view.invoice.number} viewed #{time_ago_in_words(view.created_at)} ago from #{view.location}
- else
  %p No invoices have been viewed recently.